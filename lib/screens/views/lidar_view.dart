import 'package:flutter/material.dart';

class LidarView extends StatelessWidget {
  const LidarView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('RoomPlan API Camera View\n(รอการเชื่อมต่อ Plugin หรือ Native UI)'),
    );
  }
}