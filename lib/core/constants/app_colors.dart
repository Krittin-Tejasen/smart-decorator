import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFF5F3ED);

  static const primary = Color(0xFF5F7F7B);

  static const darkText = Color(0xFF2D2D2D);

  static const lightCard = Color(0xFFE7E4DF);

  static const beige = Color(0xFFCDBEAF);

  static const white = Colors.white;

  // Redesign palette (2026) - deepened/extended version of the colors above.
  static const ink = Color(0xFF262420);
  static const muted = Color(0xFF8A8477);

  static const sage = Color(0xFF52716C);
  static const sageDeep = Color(0xFF2F433D);
  static const sageTint = Color(0xFFE3E9E6);

  static const sand = Color(0xFFDED2BE);
  static const sandTint = Color(0xFFF1EBE0);

  static const brass = Color(0xFFC6883A);
  static const brassDeep = Color(0xFF8C5F24);
  static const brassTint = Color(0xFFF6E9D5);

  static const surface = Colors.white;
}
