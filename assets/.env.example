SUPABASE_URL =
SUPABASE_ANON_KEY =

# Only needed to test on a physical device, or when the backend runs on a
# different machine than the emulator. Leave unset when running the Android
# emulator and the backend on the same machine — it defaults to 10.0.2.2.
API_BASE_URL =
